[project]
name = "tok-public-research-package"
version = "0.1.0"
description = "Public portfolio package for TOK, a governed epistemic architecture for mechanism-centered AI reasoning."
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
